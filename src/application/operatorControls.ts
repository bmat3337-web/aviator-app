export type OperatorRole="RECONCILIATION_VIEWER"|"RECONCILIATION_OPERATOR"|"RECONCILIATION_APPROVER"; export type AuditAction="CASE_ACKNOWLEDGED"|"CASE_RESOLVED"|"CORRECTION_REQUESTED"; export interface OperatorContext {operatorId:string;role:OperatorRole;requestId:string;} export interface AuditEvent {eventId:string;operatorId:string;action:AuditAction;caseId:string;requestId:string;occurredAt:string;metadata:Record<string,string>;} export function authorize(ctx:OperatorContext,action:AuditAction):void {if(!ctx.operatorId||!ctx.requestId)throw new Error("OPERATOR_CONTEXT_REQUIRED"); if(action==="CASE_ACKNOWLEDGED"&&ctx.role==="RECONCILIATION_VIEWER")throw new Error("OPERATOR_FORBIDDEN"); if(action==="CASE_RESOLVED"&&ctx.role!=="RECONCILIATION_APPROVER")throw new Error("APPROVER_REQUIRED"); if(action==="CORRECTION_REQUESTED"&&ctx.role==="RECONCILIATION_VIEWER")throw new Error("OPERATOR_FORBIDDEN");} export function buildAuditEvent(ctx:OperatorContext,action:AuditAction,caseId:string,occurredAt:string,metadata:Record<string,string>):AuditEvent{return {eventId:ctx.requestId+":"+action+":"+caseId,operatorId:ctx.operatorId,action,caseId,requestId:ctx.requestId,occurredAt,metadata};}
