export const RECONCILIATION_SCHEMA=`CREATE TABLE IF NOT EXISTS aviator_reconciliation_cases (case_id TEXT PRIMARY KEY,provider_bet_id TEXT NOT NULL,status TEXT NOT NULL,reason TEXT NOT NULL,created_at TIMESTAMPTZ NOT NULL,resolved_at TIMESTAMPTZ);`;