export const LEDGER_AND_EVENT_SCHEMA="CREATE TABLE IF NOT EXISTS aviator_game_events (event_id TEXT PRIMARY KEY,round_id TEXT NOT NULL REFERENCES aviator_rounds(round_id),sequence BIGINT NOT NULL,event_type TEXT NOT NULL,payload_json TEXT NOT NULL,occurred_at TIMESTAMPTZ NOT NULL,UNIQUE(round_id,sequence)); CREATE INDEX IF NOT EXISTS idx_aviator_events_round_sequence ON aviator_game_events(round_id,sequence);";