export const POSTGRES_SCHEMA=`CREATE TABLE IF NOT EXISTS aviator_rounds (round_id TEXT PRIMARY KEY,provider_round_id TEXT UNIQUE,sequence BIGINT NOT NULL UNIQUE,state TEXT NOT NULL,crash_multiplier NUMERIC(12,2),created_at TIMESTAMPTZ NOT NULL,closed_at TIMESTAMPTZ);
CREATE TABLE IF NOT EXISTS aviator_bets (bet_id TEXT PRIMARY KEY,round_id TEXT NOT NULL REFERENCES aviator_rounds(round_id),player_id TEXT NOT NULL,slot TEXT NOT NULL CHECK(slot IN ('BET1','BET2')),stake_minor_units NUMERIC(30,0) NOT NULL CHECK(stake_minor_units>0),state TEXT NOT NULL,auto_cash_out NUMERIC(12,2),cash_out_multiplier NUMERIC(12,2),payout_minor_units NUMERIC(30,0) NOT NULL DEFAULT 0,created_at TIMESTAMPTZ NOT NULL,settled_at TIMESTAMPTZ);
CREATE INDEX IF NOT EXISTS idx_aviator_bets_round ON aviator_bets(round_id); CREATE INDEX IF NOT EXISTS idx_aviator_bets_player ON aviator_bets(player_id);
CREATE TABLE IF NOT EXISTS aviator_idempotency (scope TEXT NOT NULL,idempotency_key TEXT NOT NULL,request_hash TEXT NOT NULL,response_status INTEGER NOT NULL,response_body TEXT NOT NULL,created_at TIMESTAMPTZ NOT NULL,expires_at TIMESTAMPTZ,PRIMARY KEY(scope,idempotency_key));
CREATE TABLE IF NOT EXISTS aviator_ledger_entries (entry_id TEXT PRIMARY KEY,account_id TEXT NOT NULL,entry_type TEXT NOT NULL,currency TEXT NOT NULL,amount_minor_units NUMERIC(30,0) NOT NULL,reference_id TEXT NOT NULL,idempotency_key TEXT NOT NULL,created_at TIMESTAMPTZ NOT NULL,UNIQUE(account_id,idempotency_key));`;
